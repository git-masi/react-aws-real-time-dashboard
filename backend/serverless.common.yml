stacks:
  infrastructure: ${cf:rardash-infrastructure-${self:provider.stage}}

shared:
  deploymentBucket: eric-experiments-k9xjhbjk3r4s
  resources:
    mainTableName: ${cf:rardash-infrastructure-${self:provider.stage}.RtkqStreamingMainName}
    mainTableArn: ${cf:rardash-infrastructure-${self:provider.stage}.RtkqStreamingMainArn}
    mainTableStreamArn: ${cf:rardash-infrastructure-${self:provider.stage}.RtkqStreamingMainStreamArn}
    mainTableIndexArns: ${cf:rardash-infrastructure-${self:provider.stage}.RtkqStreamingMainIndexArns}
    connectionsTableName: ${cf:rardash-infrastructure-${self:provider.stage}.RtkqStreamingConnectionsName}
    connectionsTableArn: ${cf:rardash-infrastructure-${self:provider.stage}.RtkqStreamingConnectionsArn}
