export const pkValues = Object.freeze({
  order: 'order',
  client: 'client',
  connection: 'connection',
});

export const orderStatuses = Object.freeze({
  open: 'open',
  cancelled: 'cancelled',
  paid: 'paid',
});
